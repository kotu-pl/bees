_target_: pytorch_lightning.loggers.WandbLogger

project: Bees
entity: kotu-pl
id: ${uuid:}
resume: never
reinit: True
save_dir: ${logs_dir}/wandb/${hydra:sweep.subdir}

name: ${now:%Y%m%d}_${hydra:job.name}_${hydra:job.num}
group: multirun_${basename:${hydra:sweep.dir}}

mode: online
job_type: train
log_model: False

tags:
  - bees
  - multirun
