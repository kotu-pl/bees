_target_: pytorch_lightning.loggers.WandbLogger

project: Bees
entity: kotu-pl
id: ${uuid:}
resume: never
reinit: True
save_dir: ${logs_dir}/wandb/${hydra:sweep.subdir}

group: multirun_${run_ts}
name: ${hydra:job.num}_${network.model.model_name}

mode: online
job_type: train
log_model: False

tags:
  - bees
  - multirun
